<% content_for :page_title, "Province" %>

<div class="card">
  <div class="card-header ">
      <div class="mb-4">

        <%= search_form_for @q, url: provinces_path, method: :get, html: { class: "row g-2 align-items-end" } do |f| %>
          <div class="col-md-1">
            <%= f.label :name_or_sku_cont, "Search", class: "form-label" %>
            <%= f.search_field :name_or_sku_cont, placeholder: "name or sku...", class: "form-control" %>
          </div>

          <div class="col-md-2">
            <%= f.submit "Search", class: "btn btn-primary" %>
          </div>
          <div class="col-md-2">
            <%= link_to new_province_path, class: "btn btn-sm btn-primary" do %>
              <i class="fas fa-plus"></i> New Province
            <% end %>
          </div>
        <% end %>
      </div>
  </div>

  <div class="card-body table-responsive p-0">
    <table class="table table-hover text-nowrap mb-0">
      <thead>
        <tr>
          <th>#</th>
          <th><%= sort_link @q, :name, "Name" %></th>
          <th><%= sort_link @q, :sku, "SKU" %></th>
          <th>Description</th>
        </tr>
      </thead>
      <tbody>
        <% @provinces.each_with_index do |province, index| %>
          <tr>
            <td><%= (@provinces.current_page - 1) * @provinces.limit_value + index + 1 %></td>
            <td><%= province.name %></td>
            <td><%= province.sku %></td>
            <td><%= province.description %></td>
            <td class="text-right">
              <%= link_to edit_province_path(province), class: "btn btn-sm btn-warning", title: "Edit" do %>
                <i class="fas fa-pencil-alt"></i>
              <% end %>
              <%= button_to province_path(province), method: :delete, form: { class: "d-inline" }, class: "btn btn-sm btn-danger province-show-delete-modal", title: "Delete", data: {
                url: province_path(province),
                name: province.name,
                sku: province.sku
              } do %>
                <i class="fas fa-trash"></i>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <div class="mt-3 d-flex justify-content-center">
      <%= paginate @provinces %>
    </div>
  </div>
</div>
