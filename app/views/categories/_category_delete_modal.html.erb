<div class="modal fade" id="category-deleteConfirmModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered modal-sm-custom">
    <div class="modal-content">
      
      <div class="modal-body text-center">
        <div class="mb-4">
          <i class="fas fa-exclamation-triangle fa-4x text-danger animate__animated animate__pulse animate__infinite"></i>
        </div>
        
        <h4 class="font-weight-bold mb-3">Are you sure?</h4>
        <p class="text-muted">
          You are about to delete <br>
          Name:<strong id="category-delete-item-name" class="text-dark"></strong>
          <br> 
          SKU:<span id="category-delete-item-sku" class="text-muted"></span>
        </p>
        <p class="text-danger small font-weight-bold">This action cannot be undone!</p>
      </div>

      <div class="modal-footer justify-content-center">
        <button type="button" class="btn btn-light" data-bs-dismiss="modal">Cancel</button>

        <form id="category-deleteConfirmForm" method="post" class="d-inline">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <input type="hidden" name="_method" value="delete">
          <button type="submit" class="btn btn-danger btn-delete-custom">Yes, Delete It</button>
        </form>
      </div>

    </div>
  </div>
</div>